# Codex prompt - setup first local CSV backtest

```text
Read AGENTS.md first and obey it.

We are in the public repository trading-bot-lab. Treat every tracked value as public.

Goal:
Verify the free local Python CSV backtesting workflow.

Scope:
- Backtesting only.
- Synthetic/demo CSV data only unless local ignored data is explicitly provided.
- No paper trading yet.
- No live trading.
- No API keys.
- No paid services.
- No real broker/exchange configuration.
- No AI, ML, neural networks, or reinforcement learning.

Tasks:
1. Inspect the repository structure.
2. Run the Python tests with `make test` or `python -m pytest`.
3. Run `python scripts/run_local_backtest.py`.
4. Confirm simulated orders go through `trading_bot_lab.risk`.
5. Do not add paid services or live-trading configuration.

Required response format:
1. Plan
2. Assumptions and risks
3. Code/docs changes
4. Tests or commands run
5. Rollback notes
```
