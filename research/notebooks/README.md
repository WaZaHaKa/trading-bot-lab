# Notebooks

Notebook files are ignored by default because they often contain outputs, data samples, and strategy IP.

When a notebook produces a durable result, summarize it in `docs/strategy-log.md` or convert it into a reproducible script.
